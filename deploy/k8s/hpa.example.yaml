# Kubernetes HPA cannot set minReplicas below 1 — this file is for "always on" baselines only.
# For scale-to-zero, prefer keda-scaledobject.example.yaml after installing KEDA.
#
# kubectl apply -f hpa.example.yaml
# (Uncomment and fix apiVersion if your metrics-server version differs.)
# apiVersion: autoscaling/v2
# kind: HorizontalPodAutoscaler
# metadata:
#   name: ehs-web
# spec:
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: ehs-web
#   minReplicas: 1
#   maxReplicas: 10
#   metrics:
#     - type: Resource
#       resource:
#         name: cpu
#         target:
#           type: Utilization
#           averageUtilization: 70

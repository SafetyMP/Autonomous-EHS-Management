# Replaces Vercel cron for GET /api/cron/data-retention (vercel.ts + src/app/api/cron/data-retention/route.ts).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: ehs-cron-data-retention
  labels:
    app.kubernetes.io/name: ehs-cron-data-retention
spec:
  # Match vercel.ts: "30 9 * * *"
  schedule: "30 9 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: curl
              image: curlimages/curl:8.11.1
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  curl -fsS \
                    -H "Authorization: Bearer ${CRON_SECRET}" \
                    "http://ehs-web:3000/api/cron/data-retention"
              env:
                - name: CRON_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: ehs-app-secrets
                      key: CRON_SECRET
